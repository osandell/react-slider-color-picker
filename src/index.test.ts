import { HueSlider, SaturationSlider, LightnessSlider, AlphaSlider } from '.'

test('HueSlider component exists', () => {
  expect(HueSlider).toBeDefined()
})

test('SaturationSlider component exists', () => {
  expect(SaturationSlider).toBeDefined()
})

test('LightnessSlider component exists', () => {
  expect(LightnessSlider).toBeDefined()
})

test('AlphaSlider component exists', () => {
  expect(AlphaSlider).toBeDefined()
})
